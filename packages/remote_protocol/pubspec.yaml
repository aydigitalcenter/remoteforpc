name: remote_protocol
description: Shared protocol definitions for Remote Mouse app
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.8

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  json_serializable: ^6.8.0
  build_runner: ^2.4.0
